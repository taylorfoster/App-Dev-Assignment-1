//: Playground - noun: a place where people can play

import Cocoa

var myString: String!
print(myString)

//It's forcing the execution even if the string is nil so functions that cause your string to go into uppercase would still attempt to run with a nil string.